class Tree {
    constructor() {
        this.root = null;
    }

    buildTree(array) {
        /*

        if (start > end) return null 
        let mid = (start + end) / 2
        let root = new Node(mid)

        root.setLeft(left half)
        root.setRight(right half)
        
        return root
        */
    }
}